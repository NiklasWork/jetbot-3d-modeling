# Current

> Update the moment a unit of work lands, not at session end. One line per item, no prose — rationale and detail belong in the owning domain file, decision, or git.
> Limits: `next:` ≤5 entries — and only until a domain file declares a `focus:`. Open points then live in that file's `next:` frontmatter and `truss status` builds the register from them; here stay `focus:` (project-wide), `blockers:` (across domains) and `branch:`. See docs/conventions.md, doctor SY-12.
> Recently done: `git log` already carries it, current and without upkeep — `truss status` prints the last commits; see git log for the rest.

focus: pipeline-3d done through package 5; the robot is on the network since 2026-09-09 — boot, WiFi, Jupyter and `basic_motion` driving the wheels from the browser are accepted, so plan-robot package 1 is done. The rest of the robot side is still written and unexecuted — `repos/robot/checkup.py` is the first thing to run on the device, and it decides whether the camera reaches 1280×960 at all. `jetbot-run.sh`'s fetch has since been run end to end against a stand-in robot, so what is still untested there is the device, not the Mac side. The driving algorithm is decided (D-014) and its package 1 — manoeuvres in `capture.py` — is built and verified against a stand-in robot; packages 2 and 3 are measurements that need the device. Engine lifted to main at rc.3+2 (`d4b14ee`). Alongside, and independent of the hardware: the JCFS presentation deck is rebuilt to 11 sections and republished (context/presentation.md), audited afterwards against the human's own list, and the one real defect the audit found is fixed: section 5 had been quoting two different benchmark runs as one. `compress` now stands its models upright before packaging them — OD-004 asks whether it should also emit the collision data the viewer needs before it lets anyone walk. On 2026-09-10 the deck was reordered on the human's choice and the opening cloud swapped from COLMAP's sparse skeleton to the trained model (context/presentation.md); it is built but **not republished**, pending his review. The same day produced the first real frames off the robot's camera, 24 of them in `captures/mount-check-2026-09-10/`, which correct one claim the deck was making and expose a blocker in `capture.py` (context/plan-robot.md package 2 and 3). The deck is published at https://claude.ai/code/artifact/dd31426b-66b2-418a-a005-e92e76306c6c — a **new** URL, because the one recorded since 2026-09-09 no longer exists. On the robot itself the dependency question is settled: `jetbot` cannot load on the host at any acceptable cost, so `Adafruit_MotorHAT` went on instead (HAT confirmed at i2c-1 0x60) and the camera goes through `cv2` + GStreamer; `repos/robot/README.md` § Our footprint carries the exact footprint. That code change is now done and run on the device: `capture.py` carries its own GStreamer camera and a motor layer built on `Adafruit_MotorHAT`, neither script imports `jetbot`, and package 3's two gating questions are answered — all five candidate resolutions deliver, and the card needs 0.106 s per frame against a ~1.0 s budget. The same run exposed a defect worth more than the measurement: the room was dark, and the blur metric scored the resulting sensor noise as sharp, so `checkup.py` proposed a threshold derived from nothing. It now checks whether a scene is in frame before it advises one, and a lit-room re-run passed that guard and produced the real threshold, `--min-sharpness 48`. That same lit run finally answered the question the deck's camera slide has carried since it was written: standing on the floor, **62.5 % of every frame is bare ground**, and the deck now says so. The frames also show OD-005 by eye, tables photographed from underneath

branch: main

blockers: none. One thing must happen at the device before a capture drive and it is not an unblock: a wheels-off-the-ground check of the motor layer, which is written and has never turned a wheel.

latent: [2026-09-08] `doctor` reports `ST-09` naming 4 engine files as locally adapted — that is upstream's stale manifest, not an edit of ours; never "restore" the engine over it. Clears when upstream cuts a release that regenerates `MANIFEST.sha256`; see TF-001
